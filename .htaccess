RewriteEngine On

# Redirect non-www and http to https://www
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^simpleloansindia\.in [NC]
RewriteRule ^(.*)$ https://www.simpleloansindia.in/$1 [L,R=301]
